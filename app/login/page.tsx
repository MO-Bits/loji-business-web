import { FlowPlaceholder } from "@/components/session/flow-placeholder";

export default function LoginPage() {
  return (
    <FlowPlaceholder
      eyebrow="Authentication"
      title="Sign in to Loji Business"
      description="The session gate correctly routed an unauthenticated user here. This placeholder will be replaced when you provide the Flutter login screen."
    />
  );
}
