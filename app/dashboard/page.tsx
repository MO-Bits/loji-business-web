import { FlowPlaceholder } from "@/components/session/flow-placeholder";

export default function DashboardPage() {
  return (
    <FlowPlaceholder
      eyebrow="Session ready"
      title="Loji Business dashboard"
      description="The user has a ready session. We will replace this with the converted MainShellScreen flow."
    />
  );
}
