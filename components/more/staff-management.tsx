"use client";

import { TeamAccessScreen } from "@/components/settings/team/team-access-screen";

/** Legacy component entry point kept for downstream imports. */
export function StaffManagement() {
  return <TeamAccessScreen />;
}
